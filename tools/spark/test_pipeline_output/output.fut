-- Generated by STUNIR
-- Module: control_flow_test
let simple_return (x: int) : int =
  x

let simple_assign (a: int) (b: int) : int =
  let result = a + b
  result

let if_else_test (x: int) : int =
  if x > 0 then x else -x

let nested_if_test (a: int) (b: int) : int =
  let max = if a > b then a else b
  max

let while_loop_test (n: int) : int =
  let sum = 0
  let i = 1
  -- while i <= n (use loop expression)
  --   let sum = sum + i
  --   let i = i + 1
  sum

let for_loop_test (n: int) : int =
  let factorial = 1
  -- for i = 1; i <= n; i++
  --   let factorial = factorial * i
  factorial

let mixed_control_flow (arr: int[]) (len: int) : int =
  let sum = 0
  let i = 0
  -- while i < len (use loop expression)
  --   let sum = sum + arr[i]
  --   let i = i + 1
  sum

let call_test (x: int) : int =
  let result = simple_return x
  result

-- End of module

