(module
  ;; Generated by STUNIR Native Core
  ;; Target: WebAssembly Text (WAT)

  ;; Import 'print' from host environment (pointer, length)
  (import "env" "print" (func $print (param i32 i32)))

  (memory 1)
  (export "memory" (memory 0))

  (func $main (export "main")
    ;; print("Hello from STUNIR!")
    i32.const 0  ;; pointer
    i32.const 18  ;; length
    call $print
  )

  ;; Data Section
  (data (i32.const 0) "Hello from STUNIR!")
)
