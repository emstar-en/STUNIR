--  STUNIR Parser Emitter - Ada SPARK Specification
--  Parser generator for multiple languages
--  DO-178C Level A compliant
--  Generated by: STUNIR Emitter Generator v1.0
--  Timestamp: 2026-01-31T02:44:18.578352Z

pragma SPARK_Mode (On);

with Emitter_Types; use Emitter_Types;

package Parser_Emitter is

   --  Parser target configuration
   type Parser_Config is record
      Target_Language : String;
      Parser_Type : String;
      Generate_Ast : Boolean;
   end record;

   --  Default configuration
   Default_Config : constant Parser_Config := (
      Target_Language => python,
      Parser_Type => recursive_descent,
      Generate_Ast => True
   );

   --  Main emission procedure with SPARK contracts
   procedure Emit_Module (
      Name      : in Identifier_String;
      Content   : out Content_String;
      Config    : in Parser_Config;
      Status    : out Emitter_Status)
      with Pre => Identifier_Strings.Length (Name) > 0,
           Post => (Status = Success) = (Content_Strings.Length (Content) > 0);

   --  Emit function code
   procedure Emit_Function (
      Func_Name : in Identifier_String;
      Params    : in String;
      Body_Code : in String;
      Content   : out Content_String;
      Status    : out Emitter_Status)
      with Pre => Identifier_Strings.Length (Func_Name) > 0;

   --  Type mapping from IR to Parser
   function Map_Type_To_PARSER (IR_Type : IR_Data_Type) return Type_Name_String;

end Parser_Emitter;
