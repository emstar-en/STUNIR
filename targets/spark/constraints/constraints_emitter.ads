--  STUNIR Constraints Emitter - Ada SPARK Specification
--  Constraint satisfaction and optimization emitter
--  DO-178C Level A compliant
--  Generated by: STUNIR Emitter Generator v1.0
--  Timestamp: 2026-01-31T02:44:17.771790Z

pragma SPARK_Mode (On);

with Emitter_Types; use Emitter_Types;

package Constraints_Emitter is

   --  Constraints target configuration
   type Constraints_Config is record
      Solver : String;
      Optimization_Level : Integer;
      Use_Globals : Boolean;
   end record;

   --  Default configuration
   Default_Config : constant Constraints_Config := (
      Solver => gecode,
      Optimization_Level => 2,
      Use_Globals => True
   );

   --  Main emission procedure with SPARK contracts
   procedure Emit_Module (
      Name      : in Identifier_String;
      Content   : out Content_String;
      Config    : in Constraints_Config;
      Status    : out Emitter_Status)
      with Pre => Identifier_Strings.Length (Name) > 0,
           Post => (Status = Success) = (Content_Strings.Length (Content) > 0);

   --  Emit function code
   procedure Emit_Function (
      Func_Name : in Identifier_String;
      Params    : in String;
      Body_Code : in String;
      Content   : out Content_String;
      Status    : out Emitter_Status)
      with Pre => Identifier_Strings.Length (Func_Name) > 0;

   --  Type mapping from IR to Constraints
   function Map_Type_To_CONSTRAINTS (IR_Type : IR_Data_Type) return Type_Name_String;

end Constraints_Emitter;
