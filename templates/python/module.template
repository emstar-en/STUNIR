#!/usr/bin/env python3
"""STUNIR: Python emission (raw target)
module: {{module_name}}
{% if docstring %}{{docstring}}
{% endif %}"""

# NOTE: This is a minimal, deterministic stub emitter.
# It preserves IR ordering and emits placeholder bodies.

{% for fn in functions_render %}
def {{fn.name}}({{fn.py_args}}):
    """{{fn.name}}"""
    # TODO: implement
    raise NotImplementedError()

{% endfor %}

if __name__ == "__main__":
    print("STUNIR module: {{module_name}}")
